# Sova Dashboard Template

Copyable starter for Sova bot dashboards.

```bash
npm install
npm run dev
npm run build
```

Use it for a new bot UI when you want the same operator layout without starting from blank Vite.

Keep domain/API code in the product repo. This template is layout only.
